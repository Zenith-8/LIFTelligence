# Generated Host App

This directory is intentionally generated by `flutter create` so you can keep developing on Windows while producing iOS builds in the cloud.

Bootstrap from `phone-app/`:

```powershell
.\tool\bootstrap.ps1
```

After bootstrapping:

```powershell
cd .\app
flutter pub get
flutter run
```

